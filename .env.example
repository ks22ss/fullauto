# Discord bot token (required for discord_client)
DISCORD_TOKEN=your_bot_token_here
CURSOR_API_KEY=your_api_key
# Optional: Redis URL for shared memory (default: redis://localhost:6379/0)
# REDIS_URL=redis://localhost:6379/0

# Optional: Scheduler interval for Agent B (e.g. cron expression or interval in minutes)
# SCHEDULER_INTERVAL_MINUTES=60

# Optional: Logging (level: DEBUG, INFO, WARNING, ERROR; file: path for log file)
# LOG_LEVEL=INFO
# LOG_FILE=logs/full_auto_agent.log
